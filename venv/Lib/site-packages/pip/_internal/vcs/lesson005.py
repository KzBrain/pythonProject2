#Task 1
f = open(r'\Users\kazzz\PycharmProjects\pythonProject\task\tnt.1','r', encoding ='utf-8')
for i in f :
    print(i,end ='')

f.close()

#Task 2
f = open(r'\Users\kazzz\PycharmProjects\pythonProject\task\2.txt','r', encoding ='utf-8')
text = f.read(26)
print(text)
text = f.readlines()
print(text)


f.close()
#Task 3
with open(r'\Users\kazzz\PycharmProjects\pythonProject\task\salary','r', encoding ='utf-8') as sal_file :
    salary = []
    poor = []
    sal_list = sal_file.read().split('\n')
    for i in sal_list :
        i = i.split()
        if float(i[1]) < 20000.0:
           poor.append(i[0])
        salary.append(i[1])
        print(f'оклад ниже 20.000 {poor},средний оклад { sum(map(float,salary))/len(salary)}')

    # Task 4
    orig = {'One': 'один', 'Two': 'два', 'Three': 'три', 'Four': 'четыре'}
    new_file = []
    with open(r'\Users\kazzz\PycharmProjects\pythonProject\task\engl.txt', 'r', encoding='utf-8') as proj_file:
        # proj_file = proj_file.read().split('\n')
        for i in proj_file:
            i = i.split()
            new_file.append(orig[i[0]] + '' + i[1])
            print(new_file)

    with open(r'\Users\kazzz\PycharmProjects\pythonProject\task\engl2.txt', 'w', encoding='utf-8') as proj2_file:
        proj2_file.writelines(new_file)
#Task 5
with open(r'\Users\kazzz\PycharmProjects\pythonProject\task\count.txt','w+') as con :
    digit = input('put the digit: \n ')


    i = digit.split()
    con.writelines(i)
    print(sum(map(int,i)))
    #try exept?

#Task 6

subj = {'Информатика': 170, 'Физика': 40,
    'Физкультура': 30}
with open(r'\Users\kazzz\PycharmProjects\pythonProject\task\schedule','r', encoding ='utf-8') as list_file:
    for list in list_file :
        subject,lecture,prac,lab = list.split()
        subj[subject] =int(lecture) + int(prac) + int(lab)
    print(f'whole time- \n {sched}')

#Task 7
import json
profit = {}
pr = {}
prof = 0
prof_aver = 0
i = 0
with open('file_7.txt', 'r') as file:
    for line in file:
        name, firm, earning, damage = line.split()
        profit[name] = int(earning) - int(damage)
        if profit.setdefault(name) >= 0:
            prof = prof + profit.setdefault(name)
            i += 1
    if i != 0:
        prof_aver = prof / i
        print(f'Прибыль средняя - {prof_aver:.2f}')
    else:
        print(f'Прибыль средняя - отсутсвует. Все работают в убыток')
    pr = {'средняя прибыль': round(prof_aver)}
    profit.update(pr)
    print(f'Прибыль каждой компании - {profit}')

with open('file_7.json', 'w') as write_js:
    json.dump(profit, write_js)

    js_str = json.dumps(profit)
    print(f'Создан файл с расширением json со следующим содержимым: \n '
          f' {js_str}')



